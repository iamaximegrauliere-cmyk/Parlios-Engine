version: "3.9"
services:
  orchestrateur:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - APP_ENV=production
    command: uvicorn src.app.main:app --host 0.0.0.0 --port 8080
